import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{LayoutDashboard,Users,ClipboardCheck,FileText,Star,Settings,Network,UserCog,Upload,Save,Search,Plus,Trash2,ExternalLink,RefreshCw,Eye,Check,Edit3,Power,ArrowRight,LockKeyhole}from'lucide-react';
import'./styles.css';

const fmt=v=>v?new Date(v).toLocaleString('ar-EG',{dateStyle:'short',timeStyle:'short'}):'غير محدد';
const errText=e=>({UNAUTHENTICATED:'انتهت جلسة الدخول.',FORBIDDEN:'ليس لديك صلاحية للوصول إلى هذا القسم.',INSUFFICIENT_PERMISSION:'ليست لديك الصلاحية المطلوبة.',POLICE_SHEET_UNAVAILABLE:'تعذر التحقق من سجل الشرطة حاليًا.',ACADEMY_STORAGE_UNAVAILABLE:'تخزين الأكاديمية غير متاح حاليًا.',STORAGE_ERROR:'تعذر حفظ البيانات حاليًا.',INVALID_TIME_RANGE:'وقت البداية والنهاية غير صحيح.',INVALID_BATCH_NAME:'اسم الدفعة غير صحيح.'}[String(e?.message||e)]||String(e?.message||e));
const api=async(url,opt={})=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),12000);try{const r=await fetch(url,{...opt,signal:c.signal,headers:{'Content-Type':'application/json',...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||`HTTP_${r.status}`);return d}catch(e){if(e.name==='AbortError')throw Error('انتهت مهلة الاتصال بالخادم.');throw e}finally{clearTimeout(t)}};
const Btn=({children,onClick,className='secondary',disabled=false})=><button className={className} disabled={disabled} onClick={onClick}>{children}</button>;
const Page=({title,sub,children})=><><section className="pageTitle"><span className="eyebrow">KAYAN POLICE ACADEMY</span><h1>{title}</h1><p>{sub}</p></section>{children}</>;
const normalizeQuestion=q=>({...q,id:String(q?.id||`q-${Date.now()}-${Math.random().toString(36).slice(2,7)}`),text:String(q?.text||''),type:['text','choice','yesno'].includes(q?.type)?q.type:'text',options:Array.isArray(q?.options)?q.options.map(String):[],correct:q?.correct==null?'':String(q.correct),required:q?.required!==false,points:Math.max(1,Number(q?.points||1))});

function AdminCenter(){const settingsOnly=location.pathname==='/settings';const[user,setUser]=useState(null),[state,setState]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[tab,setTab]=useState(settingsOnly?'settings':'dashboard'),[msg,setMsg]=useState('');const load=async()=>{setLoading(true);setError('');try{const u=await api('/api/me');setUser(u);if(!u.authenticated||!u.permissions?.isAdmin)throw Error('FORBIDDEN');setState(await api('/api/admin/state'))}catch(e){setError(errText(e))}finally{setLoading(false)}};useEffect(()=>{load()},[]);if(loading)return <div className="boot">جاري تحميل مركز الإدارة...</div>;if(error)return <div className="loginPage"><div className="loginCard"><LockKeyhole/><h1>تعذر فتح الإدارة</h1><p>{error}</p><Btn className="primary" onClick={load}><RefreshCw size={16}/> إعادة المحاولة</Btn></div></div>;const permissions=user.permissions?.adminPermissions||[];const can=p=>permissions.includes(p);const tabs=[['dashboard','نظرة عامة',LayoutDashboard,'view_dashboard'],['applications','التقديمات',FileText,'manage_applications'],['exams','الاختبارات',ClipboardCheck,'manage_exams'],['members','الأفراد',Users,'manage_members'],['admins','الأدمن والصلاحيات',UserCog,'manage_admins'],['hierarchy','هيكل الأكاديمية',Network,'manage_hierarchy'],['evaluations','التقييمات',Star,'view_evaluations'],['settings','الإعدادات',Settings,'manage_settings']].filter(x=>can(x[3]));const content=tab==='dashboard'?<Dashboard state={state}/>:tab==='applications'?<ApplicationsAdmin state={state} reload={load} setMsg={setMsg}/>:tab==='exams'?<ExamsAdmin state={state} reload={load} setMsg={setMsg}/>:tab==='members'?<MembersAdmin state={state} reload={load} setMsg={setMsg}/>:tab==='admins'?<AdminsAdmin state={state} reload={load} setMsg={setMsg}/>:tab==='hierarchy'?<HierarchyAdmin state={state} reload={load} setMsg={setMsg}/>:tab==='evaluations'?<EvaluationsAdmin state={state} reload={load} setMsg={setMsg}/>:<SettingsAdmin state={state} reload={load} setMsg={setMsg}/>;return <div className="app"><main style={{width:'100%'}}><header><div className="headerBrand"><span>مركز الإدارة</span></div><div className="userHeader"><strong>{user?.police?.name||user?.discord?.global_name||'Admin'}</strong><span className="adminTag">ADMIN</span><button className="textBtn" onClick={async()=>{await fetch('/api/logout',{method:'POST'});location.href='/'}}>خروج</button></div></header><div className="sirenBar"><div className="sirenTrack"><i/><i/><i/><i/><i/></div></div><Page title="مركز الإدارة" sub="إدارة موحدة للأفراد والتقديمات والاختبارات والهيكل والأدمن والتقييمات."><div className="adminTabs">{tabs.map(([id,label,I])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}><I size={16}/>{label}</button>)}</div>{msg&&<div className="adminMessage">{msg}</div>}{content}</Page></main></div>}

function Dashboard({state}){return <div className="adminDash"><Metric n={state.members?.length||0} t="أفراد الشرطة" I={Users}/><Metric n={state.applications?.length||0} t="طلبات التقديم" I={FileText}/><Metric n={state.exams?.length||0} t="الاختبارات" I={ClipboardCheck}/><Metric n={state.evaluations?.length||0} t="التقييمات" I={Star}/><div className="panel"><h2>حالة النظام</h2><p className="muted">مصدر بيانات الأكاديمية: Google Sheets DATA. سجل الشرطة: Police Sheet.</p><div className="detailGrid"><p><b>الدفعات:</b> {state.batches?.length||0}</p><p><b>الطلبات:</b> {state.applications?.length||0}</p><p><b>نتائج الاختبارات:</b> {state.examResults?.length||0}</p><p><b>محاولات الاختبارات:</b> {state.examAttempts?.length||0}</p></div></div></div>}
function Metric({I,n,t}){return <div className="metric"><I/><b>{n}</b><span>{t}</span></div>}

function ApplicationsAdmin({state,reload,setMsg}){const[batchForm,setBatchForm]=useState({name:'',startAt:'',endAt:''}),[editingBatch,setEditingBatch]=useState(null),[selectedBatch,setSelectedBatch]=useState(null),[selectedApp,setSelectedApp]=useState(null),[questions,setQuestions]=useState((state.applicationQuestions||[]).map(normalizeQuestion));const batches=state.batches||[],apps=state.applications||[];const grouped=useMemo(()=>{const m={};for(const a of apps)(m[a.batchId]??=[]).push(a);return m},[apps]);useEffect(()=>setQuestions((state.applicationQuestions||[]).map(normalizeQuestion)),[state.applicationQuestions]);const create=async()=>{try{await api('/api/admin/batches',{method:'POST',body:JSON.stringify(batchForm)});setBatchForm({name:'',startAt:'',endAt:''});setMsg('تم إنشاء الدفعة.');reload()}catch(e){setMsg(errText(e))}};const saveBatch=async()=>{try{await api(`/api/admin/batches/${editingBatch.id}`,{method:'PATCH',body:JSON.stringify(editingBatch)});setEditingBatch(null);setMsg('تم تعديل بيانات الدفعة.');reload()}catch(e){setMsg(errText(e))}};const toggle=async b=>{try{await api(`/api/admin/batches/${b.id}`,{method:'PATCH',body:JSON.stringify({status:b.status==='open'?'closed':'open'})});setMsg(b.status==='open'?'تم إغلاق الدفعة.':'تم فتح الدفعة.');reload()}catch(e){setMsg(errText(e))}};const saveQuestions=async()=>{try{await api('/api/admin/application',{method:'PUT',body:JSON.stringify({questions})});setMsg('تم حفظ أسئلة التقديم.');reload()}catch(e){setMsg(errText(e))}};const review=async(a,status)=>{const note=window.prompt('ملاحظات المراجع (اختياري):')??'';try{await api(`/api/admin/applications/${a.id}`,{method:'PATCH',body:JSON.stringify({status,note})});setMsg('تم تحديث الطلب.');reload()}catch(e){setMsg(errText(e))}};const remove=async a=>{if(!confirm('حذف الطلب نهائيًا؟'))return;try{await api(`/api/admin/applications/${a.id}`,{method:'DELETE'});setSelectedApp(null);setMsg('تم حذف الطلب.');reload()}catch(e){setMsg(errText(e))}};const addQ=()=>setQuestions(a=>[...a,normalizeQuestion({text:'سؤال جديد',type:'text',required:true})]);if(selectedBatch){const list=grouped[selectedBatch.id]||[];return <div className="adminSection"><div className="panel"><div className="panelHead"><div><h2>{selectedBatch.name}</h2><p className="muted">جميع المتقدمين لهذه الدفعة فقط.</p></div><Btn onClick={()=>setSelectedBatch(null)}><ArrowRight size={15}/> كل الدفعات</Btn></div>{list.map(a=><div className="applicationRow" key={a.id}><div><strong>{a.name||a.discordId}</strong><small>Discord: {a.discordId} · {fmt(a.submittedAt)}</small><small>الحالة: {a.status}{a.reviewerName?` · المراجع: ${a.reviewerName}`:''}</small></div><div className="rowActions"><Btn onClick={()=>setSelectedApp(selectedApp===a.id?null:a.id)}><Eye size={14}/> عرض تفاصيل التقديم</Btn><Btn onClick={()=>review(a,'accepted')}><Check size={14}/> قبول</Btn><Btn onClick={()=>review(a,'waitlist')}>انتظار</Btn><Btn onClick={()=>review(a,'rejected')}>رفض</Btn><button className="danger" onClick={()=>remove(a)}><Trash2 size={14}/></button></div>{selectedApp===a.id&&<ApplicationDetails app={a} questions={state.applicationQuestions||[]}/>}</div>)}{!list.length&&<div className="emptyMini">لا توجد طلبات لهذه الدفعة.</div>}</div></div>}
return <div className="adminSection"><div className="panel"><div className="panelHead"><div><h2>دفعات التقديم</h2><p className="muted">فتح وإغلاق التقديم يتم من الدفعة نفسها فقط.</p></div></div><div className="formGrid"><input value={batchForm.name} onChange={e=>setBatchForm({...batchForm,name:e.target.value})} placeholder="اسم الدفعة"/><input type="datetime-local" value={batchForm.startAt} onChange={e=>setBatchForm({...batchForm,startAt:e.target.value})}/><input type="datetime-local" value={batchForm.endAt} onChange={e=>setBatchForm({...batchForm,endAt:e.target.value})}/><Btn className="primary" onClick={create}><Plus size={16}/> إنشاء دفعة</Btn></div>{batches.map(b=><div className="applicationRow" key={b.id}><div><strong>{b.name}</strong><small>من {fmt(b.startAt)} إلى {fmt(b.endAt)} · {(grouped[b.id]||[]).length} طلب</small></div><span className={b.status==='open'?'openBadge':'closedBadge'}>{b.status==='open'?'مفتوحة':'مغلقة'}</span><div className="rowActions"><Btn onClick={()=>setSelectedBatch(b)}><Eye size={14}/> عرض التقديمات الخاصة بالدفعة</Btn><Btn onClick={()=>setEditingBatch({...b})}><Edit3 size={14}/> تعديل الدفعة</Btn><Btn onClick={()=>toggle(b)}>{b.status==='open'?'إغلاق':'فتح'}</Btn></div></div>)}</div>{editingBatch&&<div className="panel"><div className="panelHead"><h2>تعديل الدفعة</h2><Btn onClick={()=>setEditingBatch(null)}>إلغاء</Btn></div><div className="formGrid"><input value={editingBatch.name} onChange={e=>setEditingBatch({...editingBatch,name:e.target.value})}/><input type="datetime-local" value={editingBatch.startAt?.slice(0,16)||''} onChange={e=>setEditingBatch({...editingBatch,startAt:e.target.value})}/><input type="datetime-local" value={editingBatch.endAt?.slice(0,16)||''} onChange={e=>setEditingBatch({...editingBatch,endAt:e.target.value})}/></div><Btn className="primary" onClick={saveBatch}><Save size={16}/> حفظ الدفعة</Btn></div>}<div className="panel"><div className="panelHead"><h2>محرر أسئلة التقديم</h2><Btn onClick={addQ}><Plus size={16}/> إضافة سؤال</Btn></div>{questions.map((q,i)=><QuestionEditor key={q.id} question={q} onChange={v=>setQuestions(a=>a.map((x,j)=>j===i?normalizeQuestion(v):x))} onDelete={()=>setQuestions(a=>a.filter((_,j)=>j!==i))}/>) }<Btn className="primary" onClick={saveQuestions}><Save size={16}/> حفظ أسئلة التقديم</Btn></div></div>}
function ApplicationDetails({app,questions}){return <div className="panel detailPanel"><div className="detailGrid"><p><b>الاسم:</b> {app.name||'—'}</p><p><b>Discord ID:</b> {app.discordId}</p><p><b>الدفعة:</b> {app.batchId}</p><p><b>تاريخ التقديم:</b> {fmt(app.submittedAt)}</p><p><b>الحالة:</b> {app.status}</p><p><b>المراجع:</b> {app.reviewerName||'لم تتم المراجعة'}</p><p><b>ملاحظات المراجعة:</b> {app.reviewNote||'—'}</p></div><h4>تفاصيل التقديم وإجابات المتقدم</h4>{questions.map(q=><div className="answerItem" key={q.id}><strong>{q.text}</strong><span>{String(app.answers?.[q.id]??'—')}</span></div>)}{!questions.length&&Object.entries(app.answers||{}).map(([k,v])=><div className="answerItem" key={k}><strong>{k}</strong><span>{String(v)}</span></div>)}<h4>سجل الطلب</h4>{(app.history||[]).map((h,i)=><small className="historyList" key={i}>{h.status} · {fmt(h.at)} · {h.reviewerName||''} {h.note?`· ${h.note}`:''}</small>)}</div>}

function ExamsAdmin({state,reload,setMsg}){const blank={id:`new-${Date.now()}`,title:'اختبار جديد',description:'',stage:'عام',passingScore:60,durationMinutes:30,attemptsAllowed:1,startAt:'',endAt:'',active:true,questions:[]};const[editing,setEditing]=useState(null),[detail,setDetail]=useState(null),[result,setResult]=useState(null);const save=async()=>{try{const isNew=editing.id.startsWith('new-');const payload={...editing,questions:(editing.questions||[]).map(normalizeQuestion)};await api(isNew?'/api/admin/exams':`/api/admin/exams/${editing.id}`,{method:isNew?'POST':'PUT',body:JSON.stringify(payload)});setEditing(null);setMsg('تم حفظ الاختبار.');reload()}catch(e){setMsg(errText(e))}};const remove=async id=>{if(!confirm('حذف الاختبار نهائيًا؟'))return;try{await api(`/api/admin/exams/${id}`,{method:'DELETE'});setMsg('تم حذف الاختبار.');reload()}catch(e){setMsg(errText(e))}};if(detail){const results=(state.examResults||[]).filter(r=>r.examId===detail.id),attempts=(state.examAttempts||[]).filter(a=>a.examId===detail.id);return <div className="adminSection"><div className="panel"><div className="panelHead"><h2>{detail.title}</h2><Btn onClick={()=>{setDetail(null);setResult(null)}}><ArrowRight size={15}/> كل الاختبارات</Btn></div><div className="detailGrid"><p><b>الوصف:</b> {detail.description||'—'}</p><p><b>المرحلة:</b> {detail.stage||'—'}</p><p><b>درجة النجاح:</b> {detail.passingScore}%</p><p><b>المدة:</b> {detail.durationMinutes} دقيقة</p><p><b>المحاولات:</b> {detail.attemptsAllowed||1}</p><p><b>البداية:</b> {fmt(detail.startAt)}</p><p><b>النهاية:</b> {fmt(detail.endAt)}</p></div><h3>أسئلة الاختبار</h3>{(detail.questions||[]).map((q,i)=><div className="answerItem" key={q.id}><strong>{i+1}. {q.text}</strong><span>النوع: {q.type} · الدرجة: {q.points||1}{q.correct?` · الصحيحة: ${q.correct}`:''}</span></div>)}<h3>نتائج الاختبار ({results.length})</h3>{results.map(r=><div className="applicationRow" key={r.id}><div><strong>{r.name||r.userId}</strong><small>Discord: {r.userId} · {r.score}% · {r.passed?'ناجح':'غير ناجح'} · {fmt(r.submittedAt)}</small></div><Btn onClick={()=>setResult(result?.id===r.id?null:r)}><Eye size={14}/> عرض تفاصيل الاختبار</Btn>{result?.id===r.id&&<ExamResultDetails result={r} exam={detail}/>}</div>)}{!results.length&&<div className="emptyMini">لا توجد نتائج.</div>}<h3>المحاولات ({attempts.length})</h3>{attempts.map(a=><small className="historyList" key={a.id}>{a.userId} · {a.status} · بدأ {fmt(a.startedAt)} · ينتهي {fmt(a.expiresAt)}</small>)}</div></div>}
return <div className="adminSection"><div className="panel"><div className="panelHead"><h2>إدارة الاختبارات</h2><Btn className="primary" onClick={()=>setEditing({...blank,id:`new-${Date.now()}`})}><Plus size={16}/> اختبار جديد</Btn></div>{(state.exams||[]).map(e=><div className="examAdminRow" key={e.id}><div><strong>{e.title}</strong><small>{e.questions?.length||0} سؤال · {e.durationMinutes} دقيقة · {e.attemptsAllowed||1} محاولة · {fmt(e.startAt)} → {fmt(e.endAt)}</small></div><span className={e.active?'openBadge':'closedBadge'}>{e.active?'مفعل':'معطل'}</span><div className="rowActions"><Btn onClick={()=>setDetail(e)}><Eye size={14}/> عرض تفاصيل الاختبار</Btn><Btn onClick={()=>setEditing({...e,questions:(e.questions||[]).map(normalizeQuestion)})}><Edit3 size={14}/> تعديل</Btn><button className="danger" onClick={()=>remove(e.id)}><Trash2 size={14}/></button></div></div>)}{!(state.exams||[]).length&&<div className="emptyMini">لا توجد اختبارات.</div>}</div>{editing&&<ExamEditor exam={editing} setExam={setEditing} save={save} cancel={()=>setEditing(null)}/>}</div>}
function ExamEditor({exam,setExam,save,cancel}){const patch=(p)=>setExam({...exam,...p});return <div className="panel examEditor"><div className="panelHead"><h2>{exam.id.startsWith('new-')?'إنشاء اختبار':'تعديل الاختبار'}</h2><Btn onClick={cancel}>إلغاء</Btn></div><div className="formGrid"><input value={exam.title} onChange={e=>patch({title:e.target.value})} placeholder="عنوان الاختبار"/><input value={exam.description} onChange={e=>patch({description:e.target.value})} placeholder="الوصف"/><input value={exam.stage} onChange={e=>patch({stage:e.target.value})} placeholder="المرحلة"/><input type="number" min="1" max="100" value={exam.passingScore} onChange={e=>patch({passingScore:Number(e.target.value)})} placeholder="درجة النجاح"/><input type="number" min="1" value={exam.durationMinutes} onChange={e=>patch({durationMinutes:Number(e.target.value)})} placeholder="المدة بالدقائق"/><input type="number" min="1" value={exam.attemptsAllowed||1} onChange={e=>patch({attemptsAllowed:Number(e.target.value)})} placeholder="المحاولات"/><input type="datetime-local" value={exam.startAt?.slice(0,16)||''} onChange={e=>patch({startAt:e.target.value})}/><input type="datetime-local" value={exam.endAt?.slice(0,16)||''} onChange={e=>patch({endAt:e.target.value})}/></div><label className="switchLine"><input type="checkbox" checked={exam.active!==false} onChange={e=>patch({active:e.target.checked})}/> الاختبار منشور ومفعل</label>{(exam.questions||[]).map((q,i)=><QuestionEditor key={q.id} question={q} onChange={v=>patch({questions:exam.questions.map((x,j)=>j===i?normalizeQuestion(v):x)})} onDelete={()=>patch({questions:exam.questions.filter((_,j)=>j!==i)})}/>)}<Btn onClick={()=>patch({questions:[...(exam.questions||[]),normalizeQuestion({text:'سؤال جديد',type:'choice',options:['اختيار 1','اختيار 2'],required:true,points:1})]})}><Plus size={16}/> إضافة سؤال</Btn><br/><Btn className="primary" onClick={save}><Save size={16}/> حفظ الاختبار</Btn></div>}
function ExamResultDetails({result,exam}){return <div className="panel detailPanel"><div className="detailGrid"><p><b>المتقدم:</b> {result.name||result.userId}</p><p><b>Discord ID:</b> {result.userId}</p><p><b>الدرجة:</b> {result.score}%</p><p><b>الحالة:</b> {result.passed?'ناجح':'غير ناجح'}</p><p><b>وقت الإرسال:</b> {fmt(result.submittedAt)}</p></div>{(exam.questions||[]).map(q=><div className="answerItem" key={q.id}><strong>{q.text}</strong><span>الإجابة: {String(result.answers?.[q.id]??'—')} {q.correct?` · الصحيحة: ${q.correct}`:''}</span></div>)}</div>}
function QuestionEditor({question,onChange,onDelete}){const patch=p=>onChange({...question,...p});const opts=question.options||[];return <div className="panel questionAdmin"><div className="formGrid"><input value={question.text} onChange={e=>patch({text:e.target.value})} placeholder="نص السؤال"/><select value={question.type} onChange={e=>patch({type:e.target.value,options:e.target.value==='choice'?(opts.length?opts:['اختيار 1','اختيار 2']):[],correct:''})}><option value="text">نصي</option><option value="choice">اختيارات</option><option value="yesno">نعم / لا</option></select><input type="number" min="1" value={question.points||1} onChange={e=>patch({points:Number(e.target.value)})} placeholder="الدرجة"/></div>{question.type==='choice'&&<div className="questionChoices">{opts.map((o,i)=><div className="choiceEditor" key={i}><input value={o} onChange={e=>patch({options:opts.map((x,j)=>j===i?e.target.value:x),correct:question.correct===o?e.target.value:question.correct})}/><button className="danger" onClick={()=>patch({options:opts.filter((_,j)=>j!==i),correct:question.correct===o?'':question.correct})}><Trash2 size={14}/></button></div>)}<Btn onClick={()=>patch({options:[...opts,`اختيار ${opts.length+1}`]})}><Plus size={14}/> اختيار</Btn><select value={question.correct||''} onChange={e=>patch({correct:e.target.value})}><option value="">الإجابة الصحيحة</option>{opts.map((o,i)=><option key={i} value={o}>{o}</option>)}</select></div>}{question.type==='yesno'&&<select value={question.correct||''} onChange={e=>patch({correct:e.target.value})}><option value="">الإجابة الصحيحة</option><option value="نعم">نعم</option><option value="لا">لا</option></select>}<label className="checkItem"><input type="checkbox" checked={question.required!==false} onChange={e=>patch({required:e.target.checked})}/> مطلوب</label><Btn className="danger" onClick={onDelete}><Trash2 size={14}/> حذف السؤال</Btn></div>}

function MembersAdmin({state,reload,setMsg}){const[q,setQ]=useState('');const list=(state.members||[]).filter(m=>`${m.name} ${m.rank} ${m.discordId} ${m.badge}`.toLowerCase().includes(q.toLowerCase()));const upload=async(m,e)=>{const f=e.target.files?.[0];if(!f)return;if(!/^image\/(png|jpeg|webp)$/.test(f.type)){setMsg('PNG أو JPG/JPEG أو WebP فقط.');return}const r=new FileReader();r.onload=async()=>{try{await api('/api/admin/member-image',{method:'POST',body:JSON.stringify({discordId:m.discordId,image:r.result})});setMsg('تم حفظ صورة الفرد.');reload()}catch(x){setMsg(errText(x))}};r.readAsDataURL(f)};return <div className="panel"><div className="search"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث بالاسم أو Discord ID أو Badge"/></div>{list.map(m=><div className="adminMemberRow" key={m.discordId}><div className="memberAvatar">{m.image?<img src={m.image} alt=""/>:<Users size={19}/>}</div><div className="person"><strong>{m.name}</strong><small>{m.badge||'—'} · {m.rank||'—'} · {m.discordId}</small></div>{m.responsibility&&<span>{m.responsibility}</span>}<label className="upload"><Upload size={14}/> رفع صورة<input type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>upload(m,e)}/></label></div>)}{!list.length&&<div className="emptyMini">لا توجد نتائج.</div>}</div>}

function AdminsAdmin({state,reload,setMsg}){const blank={discordId:'',name:'',permissions:['view_dashboard'],enabled:true};const[form,setForm]=useState(blank),[editing,setEditing]=useState(null);const all=[['view_dashboard','لوحة الإدارة'],['manage_members','إدارة الأفراد'],['manage_roles','إدارة الرتب'],['manage_admins','إدارة الأدمن'],['manage_applications','إدارة التقديمات'],['manage_exams','إدارة الاختبارات'],['manage_hierarchy','إدارة الهيكل'],['view_evaluations','الاطلاع على التقييمات'],['manage_evaluations','إدارة التقييمات'],['manage_settings','الإعدادات']];const save=async()=>{try{await api('/api/admin/admins',{method:'POST',body:JSON.stringify(editing?form:{...form,discordId:form.discordId})});setMsg(editing?'تم تحديث الأدمن.':'تمت إضافة الأدمن.');setForm(blank);setEditing(null);reload()}catch(e){setMsg(errText(e))}};const toggle=async a=>{try{await api(`/api/admin/admins/${a.discordId}`,{method:'PATCH',body:JSON.stringify({enabled:!a.enabled})});setMsg('تم تحديث حالة الأدمن.');reload()}catch(e){setMsg(errText(e))}};const remove=async a=>{if(!confirm('حذف الأدمن؟'))return;try{await api(`/api/admin/admins/${a.discordId}`,{method:'DELETE'});setMsg('تم حذف الأدمن.');reload()}catch(e){setMsg(errText(e))}};const start=a=>{setEditing(a);setForm({discordId:a.discordId,name:a.name||'',permissions:a.permissions||[],enabled:a.enabled!==false})};return <div className="adminSection"><div className="panel"><h2>{editing?'تعديل أدمن':'إضافة أدمن'}</h2><div className="formGrid"><input value={form.discordId} disabled={Boolean(editing)} onChange={e=>setForm({...form,discordId:e.target.value})} placeholder="Discord ID"/><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="اسم الأدمن"/></div><div className="permissionGrid">{all.map(([p,l])=><label className="checkItem" key={p}><input type="checkbox" checked={form.permissions.includes(p)} onChange={()=>setForm({...form,permissions:form.permissions.includes(p)?form.permissions.filter(x=>x!==p):[...form.permissions,p]})}/>{l}</label>)}</div><label className="checkItem"><input type="checkbox" checked={form.enabled!==false} onChange={e=>setForm({...form,enabled:e.target.checked})}/> الأدمن مفعل</label><div className="rowActions"><Btn className="primary" onClick={save}><Save size={16}/> {editing?'حفظ التعديلات':'إضافة أدمن'}</Btn>{editing&&<Btn onClick={()=>{setEditing(null);setForm(blank)}}>إلغاء</Btn>}</div></div>{(state.admins||[]).map(a=><div className="panel" key={a.discordId}><div className="panelHead"><div><b>{a.name||'بدون اسم'}</b><small>{a.discordId} {a.source==='environment'?'· Super Admin من Environment':''}</small></div><div className="rowActions">{a.source!=='environment'&&<><Btn onClick={()=>start(a)}><Edit3 size={14}/> تعديل</Btn><Btn onClick={()=>toggle(a)}><Power size={14}/> {a.enabled===false?'تفعيل':'تعطيل'}</Btn><button className="danger" onClick={()=>remove(a)}><Trash2 size={14}/></button></>}</div></div><small>{(a.permissions||[]).join(' · ')||'بدون صلاحيات'}</small></div>)}</div>}

function HierarchyAdmin({state,reload,setMsg}){const[items,setItems]=useState(state.hierarchy||[]);useEffect(()=>setItems(state.hierarchy||[]),[state.hierarchy]);const patch=(i,k,v)=>setItems(a=>a.map((x,j)=>j===i?{...x,[k]:v}:x));const save=async()=>{try{await api('/api/admin/hierarchy',{method:'POST',body:JSON.stringify({items})});setMsg('تم حفظ الهيكل.');reload()}catch(e){setMsg(errText(e))}};return <div className="panel"><div className="panelHead"><h2>هيكل الأكاديمية</h2><Btn className="primary" onClick={save}><Save size={15}/> حفظ الهيكل</Btn></div>{items.map((x,i)=><div className="hierEdit" key={x.id}><div className="preview">{x.image?<img src={x.image} alt=""/>:<Network/>}</div><div className="editFields"><input value={x.title||''} onChange={e=>patch(i,'title',e.target.value)} placeholder="المسمى"/><input value={x.name||''} onChange={e=>patch(i,'name',e.target.value)} placeholder="الاسم"/><input value={x.discordId||''} onChange={e=>patch(i,'discordId',e.target.value)} placeholder="Discord ID"/><input value={x.image||''} onChange={e=>patch(i,'image',e.target.value)} placeholder="رابط الصورة"/></div></div>)}</div>}

function EvaluationsAdmin({state,reload,setMsg}){const remove=async id=>{if(!confirm('حذف التقييم؟'))return;try{await api(`/api/admin/evaluations/${id}`,{method:'DELETE'});setMsg('تم حذف التقييم.');reload()}catch(e){setMsg(errText(e))}};return <div className="panel"><div className="panelHead"><div><h2>سجل التقييمات</h2><p className="muted">للمصرح لهم فقط.</p></div><span className="ok">{state.evaluations?.length||0} تقرير</span></div>{(state.evaluations||[]).map(e=><div className="applicationRow" key={e.id}><div><strong>{e.type==='trainer_to_trainee'?'المدرب → المتدرب':'المتدرب → المدرب'}</strong><small>{e.fromName} · {e.traineeName} · {e.trainerName} · {fmt(e.createdAt)}</small><small>التقييم النهائي: {e.rating}/10</small></div><button className="danger" onClick={()=>remove(e.id)}><Trash2 size={14}/></button></div>)}</div>}

function SettingsAdmin({state,reload,setMsg}){const[s,setS]=useState({...state.settings});useEffect(()=>setS({...state.settings}),[state.settings]);const save=async()=>{try{await api('/api/admin/settings',{method:'PUT',body:JSON.stringify(s)});setMsg('تم حفظ الإعدادات.');reload()}catch(e){setMsg(errText(e))}};return <div className="panel settingsPanel"><h2>إعدادات النظام</h2><p className="muted">حالة التقديم لا تُدار من هنا. افتح أو أغلق كل دفعة من قسم التقديمات.</p><label>اسم الأكاديمية<input value={s.academyName||''} onChange={e=>setS({...s,academyName:e.target.value})}/></label><label>رابط الشعار<input value={s.logoUrl||''} onChange={e=>setS({...s,logoUrl:e.target.value})}/></label><label>عنوان التقديم<input value={s.applicationsTitle||''} onChange={e=>setS({...s,applicationsTitle:e.target.value})}/></label><label>وصف التقديم<textarea value={s.applicationsDescription||''} onChange={e=>setS({...s,applicationsDescription:e.target.value})}/></label><Btn className="primary" onClick={save}><Save size={16}/> حفظ الإعدادات</Btn></div>}

createRoot(document.getElementById('root')).render(<AdminCenter/>);
