// Evaluation controls intentionally disabled for now.
// The evaluation system is being left untouched until its backend endpoints are finalized.
// Keeping this module loaded as a no-op prevents the old UI from calling missing endpoints
// and showing NOT_FOUND alerts.
export {};